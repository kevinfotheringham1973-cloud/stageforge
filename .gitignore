# Local tooling
.claude/settings.local.json
.DS_Store

# Electron desktop shell
electron/node_modules/
electron/dist/
build-resources/
# Vendored/unused dependency tree, not read by prepare-resources.js
# (which stages from electron/node_modules instead) -- never commit
electron/pg-deps/

# Local DB backups (pg_dump) — may contain real/demo data, never commit
backups/

# Generated design canvas output (2MB+ minified editor payload,
# regenerated from design/*.dc.html + design/canvas.json via
# seed-canvas.mjs — not worth tracking or diffing).
design/stageforge-pm-sponsor-screens.html
